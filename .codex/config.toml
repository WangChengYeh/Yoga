# Project-local Codex defaults.
#
# These are the most permissive normal CLI settings:
# - no command approval prompts
# - no filesystem sandboxing
#
# Existing sessions keep their current runtime policy; restart Codex from this
# project for this file to take effect, if your launcher reads project config.
approval_policy = "never"
sandbox_mode = "danger-full-access"
